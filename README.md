# wot-tool

《坦克世界》战绩查询工具（瞄准镜军团内测版）的**自动更新托管仓库**。

| 路径 | 用途 |
|---|---|
| `version.json` | 版本清单：客户端据此比对版本、按 SHA-256 校验下载 |
| `files/v<版本>/` | 各版本的程序文件（客户端"只下变化的文件"） |
| Release 里的 zip | 首次手动下载用 |

客户端实现见 `Core/UpdateService.cs`：启动后台静默检查 → 只下变化文件 → 下次启动生效 → 失败自动回滚。

> 本仓库仅供该工具自动更新使用。
